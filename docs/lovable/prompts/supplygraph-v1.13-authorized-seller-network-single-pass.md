# Lovable single-pass prompt: SupplyGraph v1.13

Extend the existing CornerOps Control Tower only. Add an Authorized Sellers view, seller profile/detail, onboarding package review, catalog/media/inventory summaries and deterministic comparison coverage. Use the live backend contract and existing auth adapter. Show authorization basis/status, pipeline metadata, source/capture status, product count, checksum, inventory physical-count warning, approval state and audit ID.

Keep all mutation buttons disabled unless the backend explicitly reports an approved founder action. Never store secrets, crawl sellers, contact anyone, claim market completeness, show a best-supplier badge, enable quote/purchase flows, or alter CornerMex. Preserve existing structure and responsive dark UI. Use exactly one implementation pass.
